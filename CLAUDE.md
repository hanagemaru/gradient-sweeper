@AGENTS.md

# Claude Code note

Follow the shared project instructions above. GitHub `main` is the source of truth; the local
`main` checkout may be stale and does not need to be updated. Fetch GitHub before each task and
create a new `claude/` branch directly from the latest `origin/main`.

Keep work visible in a GitHub branch and pull request so Codex can continue from the same
repository state. Never share a branch with Codex or depend on unpushed local changes. Check
open pull requests and `docs/tasks/` before editing files that another session may be changing.
